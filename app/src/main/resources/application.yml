spring:
  application:
    name: DetailingStudio

  # PostgreSQL configuration
  datasource:
    url: jdbc:postgresql:///detailing_studio?cloudSqlInstance=project-f0c6dffa-7d80-4aef-9b0:europe-west3:a767891abrisa&socketFactory=com.google.cloud.sql.postgres.SocketFactory
    username: postgres
    password: numb151515!A
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

  # Spring Security
  security:
    user:
      name: admin
      password: brisa2026

server:
  port: 8080

# Application custom properties
app:
  admin:
    username: admin
    password: brisa2026
  cors:
    allowed-origins: http://localhost:5173,http://localhost:3000,https://detailing-frontend-518542558773.europe-west3.run.app
  s3:
    endpoint: ${R2_ENDPOINT:https://b570f853bda76e7e510fbe098d826a55.r2.cloudflarestorage.com}
    bucket: ${R2_BUCKET:detailing}
    access-key: ${R2_ACCESS_KEY:d88b16b1c2ffdfd6dd39e67a8cec4cc1}
    secret-key: ${R2_SECRET_KEY:3aafb13e02b0c677516857dd9a41451462cc20b8aba0b71994bbcabfb981d99e}
    region: ${R2_REGION:auto}
    public-url-prefix: ${R2_PUBLIC_URL:https://pub-your_pub_id.r2.dev}

---
# H2 profile for development without PostgreSQL
spring:
  config:
    activate:
      on-profile: h2
  datasource:
    url: jdbc:h2:file:./data/detailing_studio;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console
